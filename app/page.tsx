/**
 * Placeholder root route.
 *
 * Next requires a root page for the project to build. The real landing page is
 * Phase 5; this exists only so `next build` succeeds during Phase 1, which
 * ships the solver alone.
 */
export default function Home() {
  return <main>Compass</main>;
}
